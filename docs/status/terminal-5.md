# Terminal 5 Status

- current scope: MCP server, Streamable HTTP transport, and read-only public tool adapters
- files owned: `src/mcp_server/**`, `docs/status/terminal-5.md`
- current blocker: waiting on Terminal 3/4 concrete backend services; server currently ships with null and demo backends only
- last meaningful change: added MCP JSON-RPC dispatcher, Streamable HTTP transport, read-only tool registry, and CLI entrypoint under `src/mcp_server/`
